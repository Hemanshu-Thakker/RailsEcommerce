<section class="sandwich container">
	<div class="">
		<h3>Order confirmation</h3>
	</div>
	<% @cart_orders.each do |ord| %>
	<div class="row">
		<div class="col-lg-6">
			<p><%= ord.item.name %></p>
			<p><%= ord.quantity%></p>
			<p>sold by <%= ord.item.user.username %></p>
			<h3>Order confirmed !</h3>
		</div>
	</div>
	<% end %>
</section>