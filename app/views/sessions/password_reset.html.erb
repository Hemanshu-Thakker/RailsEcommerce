<section class="sandwich">
	<div class="container sandwich">
		<div class="row">
			<div class="col-lg-12">
				<%= form_with( local: true, url: update_password_path(@user)) do |form| %>
					<% if @user.errors.any? %>
					    <div id="error_explanation">
					      	<h2><%= pluralize(@user.errors.count, "error") %> prohibited this @user from being saved:</h2>
					      	<ul>
					      	<% @user.errors.full_messages.each do |message| %>
					        	<li><%= message %></li>
					      	<% end %>
					      	</ul>
					    </div>
					 <% end %>
					<div class="field">
					    <%= form.label :password , :class => "label-style"%>
					    <%= form.password_field :password %>
					</div>

					<div class="field">
					    <%= form.label :password_confirmation , :class => "label-style"%>
					    <%= form.password_field :password_confirmation %>
					</div>

					<div class="actions">
				    	<%= form.submit "Submit", :class => "label-style" %>
				    </div>
				<% end %>
			</div>
		</div>
	</div>
</section>